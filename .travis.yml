os: linux
dist: focal
vm: 
  create:
    name: example_root_1
before_install:
  - sudo apt-get -y install libxml2-dev
addons:
  apt:
    update: true
script:
  - echo "OK8"
jobs:
  include:
    - name: "job one"
      vm: 
        create:
          name: example_job_one_1
      script: echo "condition with job one"
    - name: "job two"
      vm: 
        create:
          name: example_job_two_2
      script: echo "condition with job two"
  
