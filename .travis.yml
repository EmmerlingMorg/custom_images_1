os: linux
dist: focal
language: generic
vm: 
  size: [large]
  create:
    name: large_im
before_install:
  - sudo apt-get -y install libxml2-dev
addons:
  apt:
    update: true
script:
  - echo $TRAVIS_COMMIT_MESSAGE
  - echo "OK"
